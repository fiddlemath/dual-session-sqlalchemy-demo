# dual-session-sqlalchemy-demo
A tiny demo/experiment of using multiple sqlalchemy sessions on the same DB at once.

output.txt is simply the command-line output I see from running `python main.py` here.
